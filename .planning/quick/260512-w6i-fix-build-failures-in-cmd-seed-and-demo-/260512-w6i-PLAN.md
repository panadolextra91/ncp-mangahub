---
phase: 260512-w6i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/seed/main.go
  - demo/mangadex-probe/main.go
  - demo/mangadex-read/main.go
  - demo/run-show/main.go
  - demo/mangadex_probe.go
  - demo/mangadex_read.go
  - demo/run_show.go
autonomous: true
requirements:
  - BUILD-FIX-01
  - BUILD-FIX-02
must_haves:
  truths:
    - "`go build ./...` exits with status 0 (no build errors)"
    - "`go vet ./...` exits with status 0"
    - "`cmd/seed` compiles and links against the actual exported constructors in `internal/infrastructure` and `internal/adapters/database`"
    - "Each demo program compiles as its own buildable command in its own subdirectory"
  artifacts:
    - path: "cmd/seed/main.go"
      provides: "Working seed program using infrastructure.NewSQLiteDB + database.NewSqliteMangaRepository"
      contains: "infrastructure.NewSQLiteDB"
    - path: "demo/mangadex-probe/main.go"
      provides: "MangaDex search probe demo (moved from demo/mangadex_probe.go)"
    - path: "demo/mangadex-read/main.go"
      provides: "MangaDex chapter reader demo (moved from demo/mangadex_read.go)"
    - path: "demo/run-show/main.go"
      provides: "Multi-protocol grand finale demo (moved from demo/run_show.go)"
  key_links:
    - from: "cmd/seed/main.go"
      to: "internal/infrastructure"
      via: "infrastructure.NewSQLiteDB(...) call"
      pattern: "infrastructure\\.NewSQLiteDB"
    - from: "cmd/seed/main.go"
      to: "internal/adapters/database"
      via: "database.NewSqliteMangaRepository(db) call"
      pattern: "database\\.NewSqliteMangaRepository"
---

<objective>
Fix the two build failures preventing `go build ./...` from succeeding so CI goes green.

Purpose: CI is red because (1) `cmd/seed/main.go` references undefined symbols `database.NewSQLiteDB` and `database.NewSQLiteMangaRepo`, and (2) the `demo/` directory has three files declaring `package main` + `func main()`, which Go forbids in a single package.

Output: A buildable repository — `go build ./...` and `go vet ./...` both return exit code 0.
</objective>

<execution_context>
@/Users/huynhngocanhthu/ncp-mangahub/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynhngocanhthu/ncp-mangahub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Verified by reading the actual source files. Use these EXACTLY — do not guess. -->

From internal/infrastructure/sqlite.go:
```go
package infrastructure

// NewSQLiteDB creates a new SQLite connection enforcing single write access and WAL mode.
func NewSQLiteDB(dsn string) (*sql.DB, error)

// InitSchema creates the necessary tables for MangaHub if they do not already exist.
func InitSchema(db *sql.DB) error
```

From internal/adapters/database/sqlite_manga_repo.go:
```go
package database

func NewSqliteMangaRepository(db *sql.DB) *sqliteMangaRepo

// Methods on *sqliteMangaRepo:
func (r *sqliteMangaRepo) Save(manga *models.Manga) error
func (r *sqliteMangaRepo) List() ([]*models.Manga, error)
```

Reference wiring from cmd/server/main.go (lines 42-57):
```go
db, err := infrastructure.NewSQLiteDB(cfg.DBPath)
if err != nil { log.Fatalf(...) }
defer db.Close()

if err := infrastructure.InitSchema(db); err != nil { log.Fatalf(...) }

mangaRepo := database.NewSqliteMangaRepository(db)
```

Note the exact casing: `Sqlite` (capital S, lowercase qlite) and `Repository` (full word, not `Repo`).
The bad code in cmd/seed used `SQLite` (all caps) and `Repo` (truncated) — neither exists.
</interfaces>

<gitnexus_note>
Per project CLAUDE.md, GitNexus rules apply. However:
- The broken files (`cmd/seed/main.go`, `demo/*.go`) currently don't compile, which means they have zero callers — `gitnexus_impact` on these is unnecessary for the fix itself.
- The constructors being WIRED INTO (`infrastructure.NewSQLiteDB`, `database.NewSqliteMangaRepository`) are NOT being modified, only called — no impact analysis required.
- However, you MUST run `gitnexus_detect_changes({scope: "staged"})` before committing, per project rules, to confirm only the expected files changed.
</gitnexus_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix cmd/seed/main.go to use correct constructor names</name>
  <files>cmd/seed/main.go</files>
  <action>
Replace the two bad calls in `cmd/seed/main.go` with the actual exported symbols, mirroring the wiring in `cmd/server/main.go`:

1. Add import `"github.com/user/mangahub/internal/infrastructure"` (keep the existing `"github.com/user/mangahub/internal/adapters/database"` import — both are needed).
2. Replace line 16 `db, err := database.NewSQLiteDB("mangahub.db")` with `db, err := infrastructure.NewSQLiteDB("mangahub.db")`.
3. Immediately after the `defer db.Close()` (line 20), add `if err := infrastructure.InitSchema(db); err != nil { log.Fatalf("❌ Failed to initialize schema: %v", err) }`. This is required because a fresh `mangahub.db` will not have the `mangas` table yet, and the existing seed would fail at `repo.Save(...)` even after fixing the build. The server does this at `cmd/server/main.go:48` — match that pattern.
4. Replace line 22 `repo := database.NewSqliteMangaRepo(db)` with `repo := database.NewSqliteMangaRepository(db)` (note the exact casing: `Sqlite` not `SQLite`, full `Repository` not `Repo`).
5. Do NOT change anything else — preserve the JSON parsing loop, log statements, and the `models.Manga` type (verify by reading the file that `m` is still a value type passed by pointer to `repo.Save(&m)`; the existing code is already correct on that front).

Do not touch any other file. Do not change function signatures elsewhere. This is a pure call-site fix.
  </action>
  <verify>
    <automated>cd /Users/huynhngocanhthu/ncp-mangahub && go build ./cmd/seed/ && go vet ./cmd/seed/</automated>
  </verify>
  <done>
`go build ./cmd/seed/` exits 0. `go vet ./cmd/seed/` exits 0. The file imports both `internal/infrastructure` and `internal/adapters/database`. The two calls reference `infrastructure.NewSQLiteDB` and `database.NewSqliteMangaRepository` respectively. `infrastructure.InitSchema(db)` is called once between `defer db.Close()` and `repo := ...`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Split demo/ into three subdirectory commands</name>
  <files>demo/mangadex-probe/main.go, demo/mangadex-read/main.go, demo/run-show/main.go, demo/mangadex_probe.go (delete), demo/mangadex_read.go (delete), demo/run_show.go (delete)</files>
  <action>
Three files in `demo/` each declare `package main` + `func main()`, which Go forbids. Split each into its own subdirectory so each becomes a buildable command (standard Go layout — same pattern as `cmd/seed` and `cmd/server`).

Steps:

1. Create directory `demo/mangadex-probe/` and move `demo/mangadex_probe.go` into it as `demo/mangadex-probe/main.go`. Use `git mv` if available so history is preserved:
   ```bash
   mkdir -p demo/mangadex-probe demo/mangadex-read demo/run-show
   git mv demo/mangadex_probe.go demo/mangadex-probe/main.go
   git mv demo/mangadex_read.go  demo/mangadex-read/main.go
   git mv demo/run_show.go       demo/run-show/main.go
   ```
   If `git mv` fails (e.g., file isn't tracked yet), fall back to regular `mv`.

2. Do NOT edit the file contents. Each file already declares `package main` and `func main()`, which is correct for its new subdirectory. The imports inside each file are already self-contained:
   - `mangadex_probe.go` uses `encoding/json`, `fmt`, `io`, `net/http`, `net/url` only — all stdlib.
   - `mangadex_read.go` uses the same stdlib set plus `encoding/base64` — all stdlib.
   - `run_show.go` uses `github.com/gorilla/websocket`, `github.com/user/mangahub/pkg/pb`, and several `google.golang.org/grpc*` packages — all already in `go.mod` (verify with `go build ./demo/run-show/` after the move; if `go.mod` is missing a dep, run `go mod tidy`).

3. After moving, verify the old `demo/` directory is empty of `.go` files at the top level:
   ```bash
   ls demo/*.go 2>/dev/null && echo "FAIL: stray files" || echo "OK: clean"
   ```
   It should print `OK: clean` (no .go files directly in `demo/`).

4. Run `go build ./...` from the repo root to confirm everything compiles. If `go mod tidy` is needed (it shouldn't be — these imports were already used by run_show.go before), run it but do not commit changes to `go.sum` unrelated to this fix without inspection.

Option B (build tags) was considered and rejected: it leaves the files un-runnable from `go run` without explicit tags and is non-idiomatic. Option A (this task) is the standard Go multi-command pattern.
  </action>
  <verify>
    <automated>cd /Users/huynhngocanhthu/ncp-mangahub && go build ./... && go vet ./... && ls demo/mangadex-probe/main.go demo/mangadex-read/main.go demo/run-show/main.go && ! ls demo/*.go 2>/dev/null</automated>
  </verify>
  <done>
`go build ./...` exits 0. `go vet ./...` exits 0. The three new files exist at `demo/mangadex-probe/main.go`, `demo/mangadex-read/main.go`, `demo/run-show/main.go`. No `.go` files remain directly under `demo/`. Each demo is independently buildable: `go build ./demo/mangadex-probe/`, `go build ./demo/mangadex-read/`, `go build ./demo/run-show/` all succeed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify full build + pre-commit scope check</name>
  <files>(none modified — verification only)</files>
  <action>
Final integration verification. Run all of the following from the repo root and confirm each exits 0:

1. `go build ./...`  — full repo build, the primary success criterion.
2. `go vet ./...`    — static analysis (catches misuse the compiler accepts).
3. `go build ./cmd/seed/`           — explicit check of the fixed seed binary.
4. `go build ./demo/mangadex-probe/` — explicit check of the relocated demo.
5. `go build ./demo/mangadex-read/`  — explicit check of the relocated demo.
6. `go build ./demo/run-show/`       — explicit check of the relocated demo (this one has the most external imports — most likely to surface go.mod issues).

If any of (3)-(6) fail with "no required module provides package" or similar, run `go mod tidy` and re-run. The expected outcome is that no tidy is needed — these imports were already satisfied before the split.

Then per project CLAUDE.md rules, run a pre-commit scope check:

```bash
git add cmd/seed/main.go demo/
gitnexus_detect_changes({scope: "staged"})
```

Expected scope of change:
- Modified: `cmd/seed/main.go` (1 file, ~4 line changes)
- Added: `demo/mangadex-probe/main.go`, `demo/mangadex-read/main.go`, `demo/run-show/main.go` (3 files, identical content to the deleted originals)
- Deleted: `demo/mangadex_probe.go`, `demo/mangadex_read.go`, `demo/run_show.go` (3 files — these may show as "renamed" if git detected the move correctly when `git mv` was used)

If `gitnexus_detect_changes` reports affected symbols OUTSIDE of `cmd/seed.main` and the three demo `main` functions, investigate — it likely means an accidental edit slipped in. The fix should NOT affect any production code paths in `internal/`, `pkg/`, or `cmd/server/`.

Do not commit. Stop here and report the build status + the detect_changes output to the user.
  </action>
  <verify>
    <automated>cd /Users/huynhngocanhthu/ncp-mangahub && go build ./... && go vet ./...</automated>
  </verify>
  <done>
Full repo `go build ./...` exits 0. Full repo `go vet ./...` exits 0. All six individual build commands above exit 0. `gitnexus_detect_changes({scope: "staged"})` shows only the expected scope (cmd/seed + demo/* paths, no other production code). User has been shown the build output and the detect_changes report.
  </done>
</task>

</tasks>

<verification>
Phase-level checks after all three tasks complete:

1. `go build ./...` — exit code 0, no output
2. `go vet ./...` — exit code 0, no output
3. `git status` — shows only the expected modified/added/deleted files (cmd/seed/main.go + demo/ reorganization)
4. The original CI errors are gone:
   - No more `undefined: database.NewSQLiteDB`
   - No more `undefined: database.NewSQLiteMangaRepo`
   - No more `main redeclared in this block`
</verification>

<success_criteria>
- `go build ./...` succeeds with exit code 0
- `go vet ./...` succeeds with exit code 0
- `cmd/seed` binary compiles using `infrastructure.NewSQLiteDB` + `database.NewSqliteMangaRepository` + `infrastructure.InitSchema`
- Three demo commands exist as independent subdirectories under `demo/` and each builds independently
- No `.go` files remain directly inside `demo/` (only subdirectories)
- `gitnexus_detect_changes({scope: "staged"})` confirms changes are scoped to the broken files only — no spillover into `internal/`, `pkg/`, or other `cmd/*` programs
- No unrelated refactors performed
</success_criteria>

<output>
After completion, create `.planning/quick/260512-w6i-fix-build-failures-in-cmd-seed-and-demo-/260512-w6i-01-SUMMARY.md` documenting:
- Files changed and why
- Exact constructor name corrections in cmd/seed
- The demo/ split rationale (Option A vs Option B)
- Build/vet output confirming green
- `gitnexus_detect_changes` output confirming scope
</output>
